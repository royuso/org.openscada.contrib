This project is a copy from https://github.com/Baseform/Baseform-Epanet-Java-Library

The UI part was not imported in order to reduce dependencies.

The required dependencies which are not already part of openSCADA External are located in
the "lib" folder of the project and are not exported.
